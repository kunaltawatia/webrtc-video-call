/**
 * Configuration
 * @module config
 */

const config = {
	name: 'WebRTC | For us',
	description:
		'A boilerplate for mern stack which enables true hot reloading and time travel.',
};

export default config;
